import { DivViewDirective } from './div-view.directive';

describe('DivViewDirective', () => {
  it('should create an instance', () => {
    const directive = new DivViewDirective();
    expect(directive).toBeTruthy();
  });
});
